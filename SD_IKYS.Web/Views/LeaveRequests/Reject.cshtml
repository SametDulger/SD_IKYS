@model SD_IKYS.Web.Models.LeaveRequestApprovalViewModel

@{
    ViewData["Title"] = "İzin Reddetme";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>İzin Reddetme</h2>
    <a href="@Url.Action("Index")" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Geri
    </a>
</div>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-body">
        <form asp-action="Reject" method="post">
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="Status" value="Rejected" />
            
            <div class="mb-3">
                <label asp-for="Comments" class="form-label">Red Notları</label>
                <textarea asp-for="Comments" class="form-control" rows="3" placeholder="Red nedenini buraya yazın..." required></textarea>
                <span asp-validation-for="Comments" class="text-danger"></span>
            </div>
            
            <div class="d-flex justify-content-end">
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-times"></i> Reddet
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 